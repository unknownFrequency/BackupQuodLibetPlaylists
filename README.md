# BackupQuodLibetPlaylists
Create a directory everyday and copy playlists to it

Add the script to your crontab. Eg. in Unix terminal

``` crontab -e ```
``` 0 15 * * * /home/username/scripts/backupQuod.sh ```
